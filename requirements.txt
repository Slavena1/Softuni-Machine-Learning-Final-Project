pandas>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.12
openpyxl>=3.1
jupyter>=1.0
nbconvert>=7.0

# Optional — only needed if babylm_zh_frequencies.csv is not present and
# frequencies must be recomputed from the raw Chinese BabyLM parquet corpus.
# jieba>=0.42
